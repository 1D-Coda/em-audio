# The reference implementation deliberately has no third-party runtime
# dependency: the evidence algebra, interval maps and operator contract are
# pure standard-library Python.  numpy and matplotlib are used only to render
# figures from the machine-readable results.
matplotlib==3.11.1
numpy==2.4.6
# neural-TTS robustness arm only
piper-tts==1.7.0
# optional: a second C2PA reader, for the transport interoperability check
c2pa-python==0.37.8
